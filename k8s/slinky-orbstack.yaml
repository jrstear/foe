apiVersion: slinky.slurm.net/v1alpha1
kind: Cluster
metadata:
  name: orbstack-slurm
  namespace: slurm
spec:
  slurmconf:
    clusterName: orbstack-slurm
  # Basic configuration for local OrbStack deployment
  # This is a placeholder and may need adjustment based on version 1.0.0 schema
